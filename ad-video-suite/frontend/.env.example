# Backend URL — override when frontend and backend run on different machines
# VITE_API_BASE_URL=http://192.168.1.10:8011

# Pre-seed the backend's campaigns folder path on first load.
# Set this to the absolute path on the backend machine so all users
# see the setting already configured without manual setup.
# VITE_BASE_PATH=/absolute/path/on/backend/machine
